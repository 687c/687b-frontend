import { useEffect } from "react";
import styled from "styled-components";



const Notification = ({ message }) => {

    useEffect(() => { });

    return (
        <div>
            <p>this is what a notification will look like</p>
        </div>
    )
}

export default Notification;